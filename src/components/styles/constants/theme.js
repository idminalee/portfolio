import * as colour from './colours'

export const lightTheme = {
  bgColour: colour.BACKGROUND,
  textColour: colour.PRIMARY,
  cardColour: colour.BACKGROUND_DARK,
}

export const darkTheme = {
  bgColour: colour.PRIMARY,
  subBgColour: colour.PRIMARY_LIGHT,
  textColour: colour.BACKGROUND,
  cardColour: colour.PRIMARY,
}
